import styled from 'styled-components';

export const SignUpContainer = styled.div`
	display: flex;
	flex-direction: column;
	width: 380px;

	p {
		color: black;
		margin: 16px 0;
	}

	span {
		color: red;
		display: inline-block;
		white-space: normal;
	}
`;

export const SignUpTitle = styled.h2`margin: 10px 0;`;
